# Yo, Listen Up! I'm Here to Drop Some Knowledge on `ts-jest`, `babel`, and `jest` Working Together Like a Dope Beat

Check it! I'm about to show you how to make these tools flow together smoother than my rhymes on "Intergalactic." This project right here? It's all about demonstrating how to get `ts-jest`, `babel`, and `jest` vibing in perfect harmony, ya know what I'm saying?

## Installation

Yo, first things first - you gotta get your dependencies locked down. It's mad simple, check it:

Run `yarn` to install dependencies

That's it! Just like that, you're ready to rock the mic (or in this case, rock your tests).

## Overview About the Configuration (The Ill Setup, Son!)

Alright, let me break down what's in this fresh project for ya:

- A `babel.config.js` - This bad boy contains my babel config. It's where the magic happens, transforming your code like I transform a beat.
- A `jest.config.js` - Now this right here is configured so `ts-jest` can read that `babel` config. It's all connected, see? Like a sick drum loop.
- A `tsconfig.json` - This joint contains the config for `typescript`. Gotta keep it type-safe while we're getting funky with our tests.

So there you have it - I set this whole thing up so you can test your TypeScript code with Jest and Babel, no doubt! It's a beautiful thing when everything works together. Now go forth and write some ill tests that'll make your code bulletproof. Peace out!
